---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const allPosts = await getCollection('posts');
const italianPosts = allPosts.filter(post => post.slug.startsWith('it/'));
const recentPosts = italianPosts
  .sort((a, b) => new Date(b.data.eventTime).getTime() - new Date(a.data.eventTime).getTime())
  .slice(0, 3);
---

<Layout lang="it">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <!-- Hero Section -->
    <div class="text-center mb-16">
      <h1 class="text-4xl md:text-5xl font-serif font-bold text-gray-900 dark:text-gray-100 mb-6">
        The Nomad Upupa
      </h1>
      <p class="text-xl text-gray-600 dark:text-gray-400 max-w-2xl mx-auto leading-relaxed">
        Un diario riflessivo senza tempo. Pensieri, luoghi e momenti che si intrecciano
        nel tempo, creando una mappa emotiva dei miei viaggi interiori ed esteriori.
      </p>
    </div>

    <!-- Recent Posts -->
    {recentPosts.length > 0 && (
      <section class="mb-16">
        <h2 class="text-2xl font-serif font-semibold text-gray-900 dark:text-gray-100 mb-8">
          Riflessioni Recenti
        </h2>
        <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
          {recentPosts.map((post) => (
            <article class="bg-white dark:bg-gray-800 rounded-lg shadow-sm hover:shadow-md transition-shadow duration-200 overflow-hidden">
              {post.data.cover && (
                <div class="aspect-video overflow-hidden">
                  <img 
                    src={post.data.cover} 
                    alt={`Copertina per ${post.data.title}`}
                    class="w-full h-full object-cover hover:scale-105 transition-transform duration-200"
                  />
                </div>
              )}
              <div class="p-6">
                <h3 class="text-lg font-serif font-semibold text-gray-900 dark:text-gray-100 mb-2">
                  <a href={`/it/posts/${post.slug.replace('it/', '')}/`} class="hover:text-warm-600 dark:hover:text-warm-400 transition-colors">
                    {post.data.title}
                  </a>
                </h3>
                <time class="text-sm text-gray-500 dark:text-gray-400 mb-3 block">
                  {new Date(post.data.eventTime).toLocaleDateString('it-IT', {
                    year: 'numeric',
                    month: 'long',
                    day: 'numeric'
                  })}
                </time>
              </div>
            </article>
          ))}
        </div>
        <div class="text-center mt-8">
          <a 
            href="/it/blog/" 
            class="inline-flex items-center px-6 py-3 bg-warm-600 dark:bg-warm-700 text-white rounded-lg hover:bg-warm-700 dark:hover:bg-warm-600 transition-colors duration-200"
          >
            Visualizza tutti i post
            <svg class="ml-2 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
            </svg>
          </a>
        </div>
      </section>
    )}

    <!-- Discover More Section -->
    <section class="text-center bg-warm-50 dark:bg-gray-800 rounded-lg p-8">
      <h2 class="text-2xl font-serif font-semibold text-gray-900 dark:text-gray-100 mb-4">
        Scopri di Più
      </h2>
      <p class="text-gray-600 dark:text-gray-400 max-w-2xl mx-auto leading-relaxed mb-6">
        Esplora la collezione completa di riflessioni, scopri i luoghi che ho visitato,
        o impara di più sul viaggio dietro queste parole.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <a 
          href="/it/blog/" 
          class="inline-flex items-center px-6 py-3 bg-warm-600 dark:bg-warm-700 text-white rounded-lg hover:bg-warm-700 dark:hover:bg-warm-600 transition-colors duration-200"
        >
          Tutte le Riflessioni
        </a>
        <a 
          href="/it/map/" 
          class="inline-flex items-center px-6 py-3 bg-gray-600 dark:bg-gray-700 text-white rounded-lg hover:bg-gray-700 dark:hover:bg-gray-600 transition-colors duration-200"
        >
          Mappa del Viaggio
        </a>
        <a 
          href="/it/about/" 
          class="inline-flex items-center px-6 py-3 bg-gray-100 dark:bg-gray-600 text-gray-700 dark:text-white rounded-lg hover:bg-gray-200 dark:hover:bg-gray-500 transition-colors duration-200"
        >
          Chi Sono
        </a>
      </div>
    </section>
  </div>
</Layout>